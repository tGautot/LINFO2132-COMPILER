
const i int = 3;
const j real = 3.2*5.0;
const k int = i*3;
const message string = "Hello";
const isEmpty bool = true;

record Point {
    x int;
    y int;
}

record Person {
    name string;
    location Point;
    history int[];
}

var a int = 3;
val e int = a*2;    // value

var c int[] = int[](5);  // new array of length 5
var d Person = Person("me", Point(3,7), int[](a*2));
c[0] = 4;
d.history[10]=20;

d.location = copyPoints(d.location);


proc square(v int) int {
    return v*v;
}

proc copyPoints(p Point) Point {
     return Point(p.x, p.y)
}

proc main() void {
    var value int = floor(5.32);
    var i int;
    for i=1 to 100 by 2 {
        while value<>3 {
            // ....
        }
    }

    i = (i+2)*2
}
